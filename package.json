{
  "name": "longevity-hallmarks-research",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:records": "node scripts/validate-records.mjs",
    "audit:references": "node scripts/audit-references.mjs",
    "verify:knowledge-base": "npm run validate:records && npm run audit:references"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
